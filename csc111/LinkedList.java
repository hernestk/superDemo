public class LinkedList {
    private int size;
    
    // public void add(E element) {
    //     insert(size(), element);
    // }
	
    public int size() {
    	return size;
    }

    // public E removeAt(int index) {
    //     if(index < 0 || index >= size()) {
    //         throw new IndexOutOfBoundsException(index);
    //     }

    //     E element; // saving the element

    //     if(index == 0) {
    //         element = head.data;
    //         head = head.next;
    //     } else {
    //         Node n = head;
    //     }
    // }

    // public boolean remove(E element) {
    //     int index = indexOf(element);
    //     if(index == -1) {
    //         return false;
    //     } else {
    //         removeAt(index);
    //         return true;
    //     }
    // }
}
